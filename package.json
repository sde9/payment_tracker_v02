{
  "name": "payment-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Учёт предоплаченных занятий для частного преподавателя",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node test/app.test.js && node test/import.test.js && node test/server.test.js"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
